<?php

namespace Config;

use CodeIgniter\Config\BaseService;

/**
 * Services Configuration file.
 *
 * Services are entities that do special tasks and provide operations inside the
 * framework. They are acts as a central location to create and store instances
 * of these helps to share them across the application.
 */
class Services extends BaseService
{
    /*
     * public static function example($getShared = true)
     * {
     *     if ($getShared) {
     *         return static::getSharedInstance('example');
     *     }
     *
     *     return new \App\Libraries\Example();
     * }
     */
}
